import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowLeft, Database, Download, GraduationCap, ShieldCheck } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { AccountMenu } from "@/auth/AccountMenu";
import { useAuth } from "@/auth/AuthContext";
import { dataCategories, downloadOwnDataExport, loadMyDataTransparencyContext, type DataStorageMode, type DataTransparencyContext } from "@/data-transparency/userDataTransparency";
import { useUserPreferences } from "@/profile/UserPreferencesContext";
import { useUserProfile } from "@/profile/UserProfileContext";

export const Route = createFileRoute("/datentransparenz")({ component: DataTransparencyPage });
function configuredStorageMode(): DataStorageMode { const authMode=import.meta.env["VITE_AUTH_MODE"]?.trim().toLowerCase(); if(authMode==="local")return "browser-local"; if(authMode==="cognito")return "cloud"; return import.meta.env.PROD?"cloud":"browser-local"; }
function policyIndependentCategories(){return dataCategories({storageMode:configuredStorageMode(),scoreVisibility:"private",leaderboardsEnabled:false,namedApprovalConfirmed:false,rawTelemetryRetentionDays:null,telemetryPseudonymizationMode:null}).filter(c=>c.id!=="scores"&&c.id!=="telemetry");}
function DataTransparencyPage(){
 const auth=useAuth(),profile=useUserProfile(),preferences=useUserPreferences(); const [context,setContext]=useState<DataTransparencyContext|null>(null),[contextError,setContextError]=useState<string|null>(null),[exporting,setExporting]=useState(false),[exportStatus,setExportStatus]=useState<string|null>(null); const identity=auth.session?.identity??null;
 useEffect(()=>{let active=true; void loadMyDataTransparencyContext().then(loaded=>{if(active){setContext(loaded);setContextError(null);}}).catch(cause=>{if(active){setContext(null);setContextError(cause instanceof Error?cause.message:"Die aktuelle Sichtbarkeits- und Aufbewahrungspolicy konnte nicht geladen werden.");}}); return()=>{active=false;};},[]);
 const categories=useMemo(()=>context?dataCategories(context):contextError?policyIndependentCategories():[],[context,contextError]);
 async function exportOwnData(){if(!identity){setExportStatus("Eigendatenexport erfordert eine aktive Anmeldung.");return;} setExporting(true);setExportStatus(null);try{await downloadOwnDataExport({identity,profile:profile.profile,preferences:preferences.preferences});setExportStatus("Dein Eigendatenexport wurde als JSON-Datei erstellt.");}catch(cause){setExportStatus(cause instanceof Error?cause.message:"Der Eigendatenexport konnte nicht erstellt werden.");}finally{setExporting(false);}}
 return <div className="min-h-screen bg-background"><header className="border-b border-border bg-panel"><div className="mx-auto flex h-14 max-w-6xl items-center gap-3 px-4 sm:px-6"><GraduationCap className="h-5 w-5 text-accent" aria-hidden="true"/><span className="text-sm font-semibold tracking-tight text-foreground">AI Training Lab</span><div className="ml-auto min-w-0"><AccountMenu compact/></div></div></header><main className="mx-auto max-w-6xl px-4 py-8 sm:px-6 sm:py-10"><Link to="/" className="inline-flex items-center gap-1.5 text-xs font-medium text-muted-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"><ArrowLeft className="h-3.5 w-3.5" aria-hidden="true"/> Meine Trainings</Link><div className="mt-5 flex flex-col gap-5 lg:flex-row lg:items-start lg:justify-between"><div className="max-w-3xl"><div className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-accent" aria-hidden="true"/><p className="text-xs font-semibold uppercase tracking-wide text-accent">Datentransparenz</p></div><h1 className="mt-2 text-2xl font-semibold tracking-tight text-foreground">Diese Daten werden über mich gespeichert</h1><p className="mt-2 text-sm leading-relaxed text-muted-foreground">Die Angaben unten beschreiben die tatsächlich vorhandenen Produktdaten, ihre Speicherorte, die technisch vorgesehenen Empfänger und die derzeit implementierten Aufbewahrungs- oder Löschregeln.</p></div><section aria-labelledby="own-data-export-title" className="w-full rounded-xl border border-border bg-panel p-4 lg:max-w-sm"><h2 id="own-data-export-title" className="text-sm font-semibold text-foreground">Eigene Daten exportieren</h2><button type="button" onClick={()=>void exportOwnData()} disabled={exporting||!identity} className="mt-4 inline-flex min-h-10 w-full items-center justify-center gap-2 rounded-md bg-accent px-3 py-2 text-xs font-semibold text-accent-foreground disabled:opacity-50"><Download className="h-4 w-4" aria-hidden="true"/>{exporting?"Export wird erstellt …":"Meine Daten als JSON exportieren"}</button>{exportStatus?<p role="status" className="mt-2 text-xs leading-relaxed text-muted-foreground">{exportStatus}</p>:null}</section></div>{contextError?<div role="alert" className="mt-6 rounded-lg border border-destructive/40 p-4 text-sm text-destructive">Die aktuelle Tenant-Policy konnte nicht sicher geladen werden. Deshalb bleiben policyabhängige Angaben ausgeblendet. {contextError}</div>:null}{!context&&!contextError?<p role="status" className="mt-6 text-sm text-muted-foreground">Sichtbarkeits- und Aufbewahrungsregeln werden geladen …</p>:null}{categories.length>0?<div data-testid="data-transparency-categories" className="mt-6 grid gap-4 lg:grid-cols-2">{categories.map(category=><section key={category.id} className="rounded-xl border border-border bg-panel p-5"><h2 className="text-base font-semibold text-foreground">{category.title}</h2><dl className="mt-4 space-y-4 text-sm"><div><dt className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Was wird gespeichert?</dt><dd className="mt-1 leading-relaxed text-foreground">{category.stored}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Wo?</dt><dd className="mt-1 leading-relaxed text-foreground">{category.storage}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Wer kann es sehen?</dt><dd className="mt-1 leading-relaxed text-foreground">{category.recipients}</dd></div><div><dt className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">Aufbewahrung / Löschung</dt><dd className="mt-1 leading-relaxed text-foreground">{category.retention}</dd></div></dl></section>)}</div>:null}</main></div>;
}
